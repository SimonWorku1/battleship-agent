node_modules/

# Persistent Agent Auth credentials — keypair + connection. Never commit.
.agent-auth.json
.agent-id

# Learned memory (ship heatmap + score history) — runtime artifact, per-machine.
.agent-memory.json

# Logs / scratch
*.log
